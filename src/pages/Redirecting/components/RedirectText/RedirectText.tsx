import * as constants from "./constants";
import * as styles from "./styles";
import { Text } from "@chakra-ui/react";

const RedirectText = () => {
  return <Text {...styles.$textStyle0}>{constants.text1}</Text>;
};
export default RedirectText;
