import Lottie from "lottie-react";
import addedAnimation from "@/theme/animations/added-animation.json";

const AddedAnimation = () => {
  return <Lottie animationData={addedAnimation} loop={false} />;
};

export default AddedAnimation;
